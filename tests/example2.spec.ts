import { test, expect } from '@playwright/test';

test('verify valid Login in Lambatest', async ({ page }) => {

await  page.goto('https://www.lambdatest.com/selenium-playground').click('Drag & Drop Sliders')});
